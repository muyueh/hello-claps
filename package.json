{
  "name": "hello-claps",
  "version": "1.0.0",
  "description": "Helper scripts for Google Apps Script projects managed with clasp.",
  "license": "ISC",
  "type": "commonjs",
  "scripts": {
    "login": "clasp login",
    "login:no-localhost": "clasp login --no-localhost",
    "pull": "clasp pull",
    "push": "clasp push",
    "open": "clasp open",
    "deploy": "clasp deploy",
    "survey:create": "CI=1 clasp run createOrUpdateSurveySheet",
    "survey:summary": "CI=1 clasp run runSurveyReport"
  },
  "devDependencies": {
    "@google/clasp": "^3.1.1"
  }
}
